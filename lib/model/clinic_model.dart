class ClinicModel {
  String? clinic_name;
  String? clinic_address;
  String? clinic_image;
  String? url;

  ClinicModel(
      this.clinic_name, this.clinic_address, this.clinic_image, this.url);
}
